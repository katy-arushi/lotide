/**
 * a function which accepts an array as an argument, and returns the head (item at index 0) of the array
 * @param {array} myArray
 * @returns {myArray[0]} head of the array
 */

const head = function(myArray) {
  return myArray[0];
};

module.exports = head;